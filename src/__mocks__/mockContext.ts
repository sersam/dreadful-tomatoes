import { ShowTypes } from '../constants';

const mockContext = {
  showType: ShowTypes.MOVIES,
  setShowType: () => {},
  title: '',
  setTitle: () => {},
  releaseYear: null,
  setReleaseYear: () => {},
};

export { mockContext };
